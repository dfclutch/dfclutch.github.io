/**
 * Typography style constants for usage in styled components
 */

import colors from "./colors";

const mainTitle = `
  font-size: 2.9rem;
`;

const title = `
  font-size: 2.6rem;
`;

const large = `
  font-size: 1.7rem;
`;

const medium = `
  font-size: 1.4rem;
`;

const small = `
  font-size: 1.2rem;
`;

const extra_small = `
  font-size: .9rem;
`;

const serif = `
  font-family: "Spectral", serif;
`;

const sans_serif = `
  font-family: "Roboto", Helvetica, sans-serif;
`;

export default {
  large,
  mainTitle,
  medium,
  extra_small,
  serif,
  sans_serif,
  small,
  title,
};
